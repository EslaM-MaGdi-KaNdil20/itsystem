# نظام التنبيهات بالبريد الإلكتروني للاشتراكات

## الإعدادات المطلوبة في ملف .env

```env
# Email Configuration for Subscription Alerts
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_RECIPIENT=your-email@gmail.com
```

## كيفية إعداد البريد الإلكتروني (Gmail)

### 1. تفعيل المصادقة الثنائية (2FA)
- اذهب إلى حساب Google الخاص بك
- اختر "الأمان" (Security)
- فعّل "التحقق بخطوتين" (2-Step Verification)

### 2. إنشاء كلمة مرور التطبيق (App Password)
- في صفحة الأمان، ابحث عن "كلمات مرور التطبيقات" (App Passwords)
- اختر "البريد" (Mail) و "جهاز آخر" (Other device)
- قم بتسمية التطبيق: "IT System Alerts"
- انسخ كلمة المرور المكونة من 16 رقماً
- استخدمها في `EMAIL_PASSWORD` في ملف .env

### 3. تحديث ملف .env
```env
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-actual-email@gmail.com
EMAIL_PASSWORD=xxxx xxxx xxxx xxxx  # كلمة المرور من الخطوة السابقة (بدون مسافات)
EMAIL_RECIPIENT=your-actual-email@gmail.com  # البريد الذي سيستقبل التنبيهات
```

## كيف يعمل النظام؟

### 1. الفحص التلقائي
- يتم فحص الاشتراكات **يومياً في الساعة 9:00 صباحاً**
- يبحث عن الاشتراكات النشطة التي ستنتهي خلال **30 يوماً**

### 2. إرسال التنبيهات
- عند العثور على اشتراك قريب من الانتهاء:
  - يتم إرسال إيميل تنبيه تلقائياً
  - يحتوي الإيميل على:
    * اسم الاشتراك
    * عدد الأيام المتبقية
    * تاريخ الانتهاء
    * تفاصيل الاشتراك (المزود، التكلفة، الخدمة)

### 3. محتوى الإيميل
- تصميم احترافي باللغة العربية
- عرض واضح لعدد الأيام المتبقية
- جميع تفاصيل الاشتراك بشكل منظم
- تنبيه بصري واضح

## الاختبار اليدوي

إذا كنت تريد اختبار النظام فوراً بدون انتظار الموعد اليومي:

```javascript
// في ملف server.js، يمكنك تغيير الموعد لاختبار فوري:
// من:
cron.schedule('0 9 * * *', ...);  // يومياً في 9 صباحاً

// إلى:
cron.schedule('*/5 * * * *', ...);  // كل 5 دقائق للاختبار
```

## استكشاف الأخطاء

### لا يتم إرسال الإيميلات؟
1. تأكد من صحة بيانات البريد في .env
2. تحقق من تفعيل المصادقة الثنائية وكلمة مرور التطبيق
3. راجع سجل الأخطاء في console:
   ```
   ✅ Email server is ready to send messages  // البريد يعمل
   ❌ Email server connection failed          // خطأ في الإعدادات
   ```

### استخدام بريد آخر غير Gmail؟

#### Outlook/Hotmail:
```env
EMAIL_HOST=smtp-mail.outlook.com
EMAIL_PORT=587
```

#### Yahoo:
```env
EMAIL_HOST=smtp.mail.yahoo.com
EMAIL_PORT=587
```

#### بريد مخصص:
```env
EMAIL_HOST=smtp.your-domain.com
EMAIL_PORT=587  # أو 465 للاتصال الآمن
```

## ملاحظات مهمة

1. **الأمان**: لا تشارك كلمة مرور التطبيق مع أحد
2. **السجلات**: يتم تسجيل جميع التنبيهات في console
3. **التشغيل التلقائي**: يبدأ الفحص تلقائياً عند بدء السيرفر
4. **الفحص الأولي**: يتم فحص أولي بعد 5 ثواني من تشغيل السيرفر

## أمثلة على رسائل السجل (Console Logs)

```
🚀 Starting subscription expiration checker...
✅ Email server is ready to send messages
✅ Subscription checker scheduled (runs daily at 9:00 AM)
🔄 Running initial subscription check...
🔍 Checking for expiring subscriptions...
⚠️ Found 2 subscription(s) expiring within 30 days
📧 Sending alert for: Microsoft 365 (25 days remaining)
✅ Email sent for subscription: Microsoft 365
📧 Sending alert for: Adobe Creative Cloud (15 days remaining)
✅ Email sent for subscription: Adobe Creative Cloud
```
