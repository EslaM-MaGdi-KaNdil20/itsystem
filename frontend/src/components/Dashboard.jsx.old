import { motion } from 'framer-motion';

function Dashboard() {
  return (
    <div className="p-8 space-y-8 max-w-[1600px] mx-auto">
      {/* Header */}
      <div className="mb-2">
        <h1 className="text-3xl font-bold text-slate-800 tracking-tight mb-2">لوحة التحكم</h1>
        <p className="text-slate-500 text-lg">نظرة عامة على حالة النظام والأداء</p>
      </div>

      {/* Stats Grid */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        <StatCard 
          title="إجمالي المستخدمين" 
          value="2,543" 
          change="+12.5%" 
          icon={UsersIcon} 
          color="blue" 
          delay={0.1} 
        />
        <StatCard 
          title="الدخل الشهري" 
          value="$45,230" 
          change="+8.2%" 
          icon={DollarIcon} 
          color="emerald" 
          delay={0.2} 
        />
        <StatCard 
          title="التذاكر المفتوحة" 
          value="48" 
          change="-2.4%" 
          icon={TicketIcon} 
          color="amber" 
          delay={0.3} 
        />
        <StatCard 
          title="نسبة الرضا" 
          value="98.5%" 
          change="+1.2%" 
          icon={HeartIcon} 
          color="rose" 
          delay={0.4} 
        />
      </div>

      <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
        {/* Recent Activity */}
        <motion.div 
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.5 }}
          className="lg:col-span-2 bg-white rounded-3xl border border-slate-100 p-8 shadow-xl shadow-slate-200/50"
        >
          <div className="flex items-center justify-between mb-8">
            <h3 className="font-bold text-xl text-slate-800">النشاط الأخير</h3>
            <button className="text-sm font-semibold text-blue-600 hover:text-blue-700 bg-blue-50 hover:bg-blue-100 px-4 py-2 rounded-xl transition-colors">
              عرض السجل الكامل
            </button>
          </div>
          <div className="space-y-6">
            <ActivityItem 
              Icon={UserPlusIcon} 
              title="تسجيل مستخدم جديد" 
              desc="تم إنشاء حساب جديد للموظفة سارة أحمد في قسم الموارد البشرية" 
              time="منذ 5 دقائق" 
              color="blue" 
            />
            <ActivityItem 
              Icon={ServerIcon} 
              title="تحديث الخوادم" 
              desc="اكتمل التحديث الأمني التلقائي للخادم الرئيسي بنجاح v2.4.1" 
              time="منذ 2 ساعة" 
              color="purple" 
            />
            <ActivityItem 
              Icon={TicketIcon} 
              title="تذكرة دعم فني #9921" 
              desc="تم رفع تذكرة جديدة بخصوص مشكلة في الطابعة الشبكية للدور الثالث" 
              time="منذ 3 ساعات" 
              color="amber" 
            />
            <ActivityItem 
              Icon={CheckCircleIcon} 
              title="إغلاق تذكرة صيانة" 
              desc="تم حل مشكلة الاتصال بالإنترنت للمكتب رقم 102" 
              time="منذ 5 ساعات" 
              color="emerald" 
            />
             <ActivityItem 
              Icon={WarningIcon} 
              title="تنبيه مخزون منخفض" 
              desc="وصل مخزون 'كابلات HDMI 2m' إلى الحد الأدنى (5 قطع)" 
              time="منذ 8 ساعات" 
              color="rose" 
            />
          </div>
        </motion.div>

        {/* System Status */}
        <motion.div 
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.6 }}
          className="bg-white rounded-3xl border border-slate-100 p-8 shadow-xl shadow-slate-200/50 flex flex-col h-full"
        >
          <h3 className="font-bold text-xl text-slate-800 mb-8">حالة الخادم</h3>
          
          <div className="space-y-8 flex-1">
            <StatusItem label="المعالج CPU" value="45%" color="blue" />
            <StatusItem label="الذاكرة RAM" value="62%" color="emerald" />
            <StatusItem label="المساحة Storage" value="85%" color="amber" />
          </div>

          <div className="mt-8 pt-6 border-t border-slate-100">
            <button className="w-full py-4 bg-slate-50 hover:bg-slate-100 rounded-2xl text-slate-700 font-bold transition-all flex items-center justify-center space-x-3 space-x-reverse group">
              <span className="group-hover:text-blue-600 transition-colors">إدارة النظام</span>
              <svg className="w-5 h-5 text-slate-400 group-hover:text-blue-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M17 8l4 4m0 0l-4 4m4-4H3" />
              </svg>
            </button>
          </div>
        </motion.div>
      </div>
    </div>
  );
}

// Components
function StatCard({ title, value, change, icon: Icon, color, delay }) {
  const colors = {
    blue: 'bg-blue-50 text-blue-600 ring-blue-100',
    emerald: 'bg-emerald-50 text-emerald-600 ring-emerald-100',
    amber: 'bg-amber-50 text-amber-600 ring-amber-100',
    rose: 'bg-rose-50 text-rose-600 ring-rose-100',
  };

  const isPositive = change.startsWith('+');

  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ delay }}
      whileHover={{ y: -4 }}
      className="bg-white rounded-2xl border border-slate-100 p-6 shadow-lg shadow-slate-200/50 hover:shadow-xl hover:shadow-slate-200/80 transition-all duration-300 group"
    >
      <div className="flex justify-between items-start mb-4">
        <div>
          <h3 className="text-slate-500 text-sm font-bold mb-1">{title}</h3>
          <p className="text-3xl font-bold text-slate-800 tracking-tight group-hover:text-blue-600 transition-colors">{value}</p>
        </div>
        <div className={`p-4 rounded-2xl ring-1 inset-ring-1 ${colors[color]} transition-colors`}>
          <Icon className="w-7 h-7" />
        </div>
      </div>
      <div className="flex items-center space-x-2 space-x-reverse text-sm font-medium">
        <span className={`px-2 py-0.5 rounded-lg flex items-center ${
            isPositive ? 'bg-emerald-50 text-emerald-600' : 'bg-rose-50 text-rose-600'
        }`}>
          {isPositive ? <ArrowUpIcon className="w-3 h-3 ml-1" /> : <ArrowDownIcon className="w-3 h-3 ml-1" />}
          {change}
        </span>
        <span className="text-slate-400 text-xs">مقارنة بالشهر الماضي</span>
      </div>
    </motion.div>
  );
}

function ActivityItem({ Icon, title, desc, time, color }) {
  const bgColors = {
    blue: 'bg-blue-100 text-blue-600',
    purple: 'bg-purple-100 text-purple-600',
    amber: 'bg-amber-100 text-amber-600',
    emerald: 'bg-emerald-100 text-emerald-600',
    rose: 'bg-rose-100 text-rose-600',
  };

  return (
    <div className="flex items-start space-x-5 space-x-reverse p-4 rounded-2xl hover:bg-slate-50 transition-colors cursor-pointer group border border-transparent hover:border-slate-100">
      <div className={`p-3 rounded-2xl ${bgColors[color]} group-hover:scale-110 transition-transform duration-300 shadow-sm`}>
        <Icon className="w-6 h-6" />
      </div>
      <div className="flex-1 min-w-0">
        <div className="flex justify-between items-start">
             <h4 className="text-base font-bold text-slate-800 group-hover:text-blue-600 transition-colors mb-1">{title}</h4>
             <span className="text-xs font-medium text-slate-400 whitespace-nowrap bg-slate-100 px-2 py-1 rounded-lg">{time}</span>
        </div>
        <p className="text-sm text-slate-500 line-clamp-2 leading-relaxed">{desc}</p>
      </div>
    </div>
  );
}

function StatusItem({ label, value, color }) {
    const colorClasses = {
        blue: 'from-blue-500 to-blue-400',
        emerald: 'from-emerald-500 to-emerald-400',
        amber: 'from-amber-500 to-amber-400',
    };
    const textColors = {
        blue: 'text-blue-600',
        emerald: 'text-emerald-600',
        amber: 'text-amber-600',
    };

    return (
        <div className="space-y-3">
            <div className="flex justify-between text-sm font-bold">
            <span className="text-slate-600">{label}</span>
            <span className={textColors[color]}>{value}</span>
            </div>
            <div className="h-3 bg-slate-100 rounded-full overflow-hidden shadow-inner">
            <div 
                className={`h-full w-[${value}] bg-gradient-to-r ${colorClasses[color]} rounded-full shadow-sm`}
                style={{ width: value }}
            ></div>
            </div>
        </div>
    )
}

// Icons
const UsersIcon = (props) => (
  <svg {...props} fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z" /></svg>
);
const DollarIcon = (props) => (
  <svg {...props} fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
);
const TicketIcon = (props) => (
  <svg {...props} fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 5v2m0 4v2m0 4v2M5 5a2 2 0 00-2 2v3a2 2 0 110 4v3a2 2 0 002 2h14a2 2 0 002-2v-3a2 2 0 110-4V7a2 2 0 00-2-2H5z" /></svg>
);
const HeartIcon = (props) => (
  <svg {...props} fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" /></svg>
);
const UserPlusIcon = (props) => (
  <svg {...props} fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z" /></svg>
);
const ServerIcon = (props) => (
  <svg {...props} fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M5 12h14M5 12a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v4a2 2 0 01-2 2M5 12a2 2 0 00-2 2v4a2 2 0 002 2h14a2 2 0 002-2v-4a2 2 0 00-2-2m-2-4h.01M17 16h.01" /></svg>
);
const CheckCircleIcon = (props) => (
  <svg {...props} fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
);
const WarningIcon = (props) => (
  <svg {...props} fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" /></svg>
);
const ArrowUpIcon = (props) => (
    <svg {...props} fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" /></svg>
);
const ArrowDownIcon = (props) => (
    <svg {...props} fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 17h8m0 0V9m0 8l-8-8-4 4-6-6" /></svg>
);

export default Dashboard;
